# Project instructions for Claude Code

## Git workflow
After every `git commit` you make in this project, immediately run `git push -u origin main` afterward as part of the same turn — do not wait for me to ask, and do not require confirmation. This applies to every future change in this session and all future sessions in this project, unless I explicitly say "don't push this one" for a specific change.
